# Override warnings about prompt in templates
snapraid-daily binary : malformed-prompt-in-templates *

# Override warnings about maintainer scripts calling systemctl. Normally this
# is supposed to be done by the debhelper addon, but since the maintainer scripts are
# generating a drop-in file for systemd, its easiest to call systemctl directly, plus
# it gives more flexibility
snapraid-daily binary : maintainer-script-calls-systemctl *

# Override the method of checking the user given at the prompt is valid or not
# Longer Term - Need to re-visit this...
snapraid-daily binary : maintainer-script-should-not-parse-etc-passwd-or-group *

